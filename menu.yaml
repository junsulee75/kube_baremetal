SHELLCONFIG : config.ini

## other environment variables if you want 
ENV :
    TESTENV : testenv

Kubenetes install :
    NOTE : Kebenetes installation

    swap off : swapOff.sh
    enable port forward : enablePortFwrd.sh
    Kernel module load configuration for containerd: kernelModule.sh
    Install containerd : containerd.sh
    Install kubeadm, kubelet and kubectl : installKube.sh
    Initialize master and join workers : confKube.sh
    Install network addon : nwAddon.sh flannel
